---
version: 5
defaults:
  datadir: /tmp/vagrant-puppet/hieradata/
  data_hash: yaml_data
hierarchy:
  - name: "Per-node"
    path: "%{::hostname}.yaml"
  - name: "role"
    paths:
      - "role/%{::role}.yaml"
      - "common.yaml"
